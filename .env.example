# Binance USD-M Futures API credentials.
# For paper mode these are OPTIONAL (only public market data is used).
# For live mode create keys with FUTURES TRADING enabled and WITHDRAWALS DISABLED.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Set to "true" to use the Binance Futures testnet endpoints.
BINANCE_TESTNET=false
